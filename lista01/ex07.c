#include <stdio.h>

    int main(){

        int a;

        printf("Type in a number to be inverted: ");
        scanf("%d", &a);
        
        printf("The number is now reversed: %d\n", -a);

        }
    }